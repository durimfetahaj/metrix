export interface MenuItem {
  label: string;
  url: string;
}

export interface Product {
  description: string;
  name: string;
  images: string[];
  price: string;
  stock: string;
  category: string;
}
